<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  x:Class="EIJ.Views.VoicesPage"
  xmlns="http://xamarin.com/schemas/2014/forms"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:d="http://xamarin.com/schemas/2014/forms/design"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewModels="clr-namespace:EIJ.ViewModels;assembly=EIJ"
  mc:Ignorable="d">

  <ContentPage.BindingContext>
    <viewModels:VoicesViewModel />
  </ContentPage.BindingContext>

  <ContentPage.Content>
    <StackLayout>
<ListView ItemSelected="{Binding OnItemSelected}" 
          ItemsSource="{Binding Voices, Mode=OneWay}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <Label Text="{Binding Path=PlayerName}" />
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>